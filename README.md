# Neyra Business Agent Network

Закрытая продуктовая основа для агентной бизнес-системы: центральный оператор Нэйра маршрутизирует задачи изолированным специалистам и принимает результат только с evidence.

> **Статус:** private foundation. Репозиторий содержит только переносимые исходники, демо-данные и контракты. Клиентские контуры, секреты и рабочие данные сюда не входят.

## Состав сети

| Компонент | Роль | Результат |
|---|---|---|
| Orchestrator | Принимает задачу, проверяет границы и выбирает исполнителя | task envelope и проверенный итог |
| Legal Agent | Разбирает юридические документы и риски в разрешённой юрисдикции | заключение с источниками, ограничениями и evidence |
| Finance Agent | Работает с финансовыми моделями, таблицами и платёжными сценариями | расчёт, файлы и проверяемый вывод |
| Shared contracts | Определяют форматы task, handoff, decision, risk и evidence | совместимость агентов без передачи лишних данных |

## Архитектура

```text
Telegram / Web / API
        │
        ▼
 Neyra Orchestrator
        │
 ┌──────┼──────────────┐
 ▼      ▼              ▼
Legal  Finance    Future agents
        │
        ▼
Shared contracts → evidence → handoff → verified result
```

Подробнее: [архитектура](docs/architecture.md), [модель безопасности](docs/security-model.md), [правила публикации](docs/publishing-gate.md).

## Репозиторий

- `agents/` — переносимые описания ролей, policies и capability manifests.
- `shared/schemas/` — JSON Schema для межагентного обмена.
- `demo/` — только синтетические примеры.
- `docs/` — архитектура, безопасность и развёртывание.
- `tests/` — contract и integration fixtures.

## Локальная проверка

```bash
make check
```

Команда валидирует JSON-контракты и проверяет, что обязательные файлы присутствуют. Runtime и production deployment будут добавляться отдельными проверяемыми изменениями.

## Публичная публикация

Этот репозиторий создаётся private. Переход в public возможен только после прохождения [publishing gate](docs/publishing-gate.md), отдельного решения о лицензии и проверки происхождения каждого переносимого компонента.
